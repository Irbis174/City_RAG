[project]
name = "frontend"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "httpx>=0.28.1",
    "pydantic>=2.13.5",
    "python-dotenv>=1.2.3",
    "shared",
    "streamlit>=1.63.0",
]

[tool.uv.sources]
shared = { path = "../shared", editable = true }
